@model SimpleQuizApp.Models.QuizViewModel

<div class="container text-center mt-5">
    <h2>Résultat</h2>
    <p>Vous avez obtenu un score de <strong>@Model.Score</strong> sur <strong>@Model.TotalQuestions</strong>.</p>
    <a href="/Quiz/Start" class="btn btn-primary mt-3">Rejouer</a>
</div>
